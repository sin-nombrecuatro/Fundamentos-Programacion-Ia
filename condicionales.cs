// Un condicional es instrucción que permite a un programa tomar decisiones y elegir qué camino seguir, dependiendo de si se cumple o no una regla específica

// Estospermiten a un programa tomar decisiones y cambiar su camino de ejecución según si una condición es verdadera o falsa

//Ejemplo

if (key.input.down("space")) {
    // Ejecutar acción si se presiona la barra espaciadora
    jump();
} else {
    // Ejecutar acción si no se presiona la barra espaciadora
    walk();
}
